server:

 port: 8081

spring:
 application:
    name: new-sale-service 
 datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Otsi@123
 jpa:
   hibernate:
    ddl-auto: update
   database-platform: org.hibernate.dialect.PostgreSQL9Dialect
   show-sql: true
   
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8762/eureka/
  instance:
    instance-id: ${spring.application.name}:${random.int}
    hostname: localhost
logging:
    file:
      name: C:/Users/ADMIN/Desktop/logs/newsale.log    
    level:
      com.otsi.retail.newSale.serviceimpl: DEBUG   
springdoc:
  packagesToScan: com.otsi.retail.newSale
  
savecustomer:
  url: http://localhost:8081/customer/savecustomerdetails
  
getNewSaleWithHsn: 
  url: http://localhost:8083/hsnDetails/getHsnDetails