apiVersion: v1
kind: Service
metadata:
  name: {{ include "new-sale-api.fullname" . }}
  labels:
    {{- include "new-sale-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "new-sale-api.selectorLabels" . | nindent 4 }}
