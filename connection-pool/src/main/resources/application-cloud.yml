server:
 port: 8085
 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://10.100.143.45:8762/eureka/
  instance:
    hostname: localhost
  
spring:
 application:
    name: connection-pool
 datasource:
    platform: postgres
    url: jdbc:postgresql://10.100.149.140:5432/promotions
    username: postgres
    password: Otsi@123
 jpa:
   hibernate:
    ddl-auto: update
   database-platform: org.hibernate.dialect.PostgreSQL9Dialect
   show-sql: true
logging:
    file:
      name: C:/Users/ADMIN/Desktop/logs/connection-pool1.log
    level:
      com.otsi.retail.connectionpool.service: DEBUG  
          