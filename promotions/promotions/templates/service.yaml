apiVersion: v1
kind: Service
metadata:
  name: {{ include "promotions.fullname" . }}
  labels:
    {{- include "promotions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "promotions.selectorLabels" . | nindent 4 }}
