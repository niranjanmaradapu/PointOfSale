apiVersion: v1
kind: Service
metadata:
  name: {{ include "promotions.fullname" . }}-pos
  labels:
    {{- include "promotions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.postgres.type }}
  ports:
    - port: {{ .Values.service.postgres.port }}
  selector:
    {{- include "promotions.selectorLabels" . | nindent 4 }}
