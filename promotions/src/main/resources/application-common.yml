server:
 port: 9090
 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
  
spring:
 application:
    name: connection-pool
 datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/connection-pool
    username: postgres
    password: Otsi@123
 jpa:
   hibernate:
    ddl-auto: update
   database-platform: org.hibernate.dialect.PostgreSQL9Dialect
   show-sql: true
logging:
    file:
      name: C:/Users/ADMIN/Desktop/logs/connection-pool.log
    level:
      com.otsi.retail.connectionpool.service: DEBUG  